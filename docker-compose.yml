services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-website
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: '1'
